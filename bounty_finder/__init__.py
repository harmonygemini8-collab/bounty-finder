"""bounty-finder: a read-only assistant that discovers and scores bounty issues.

This package does NOT post comments, claim bounties, or open pull requests.
It only reads public data and produces a ranked shortlist so a human can decide
which issues are worth pursuing.
"""

__version__ = "0.1.0"
